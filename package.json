{
  "name": "practicafront",
  "version": "1.0.0",
  "homepage": "https://rodrigok10.github.io/rodrigo-portafolio/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'No lint setup yet' && exit 0",
    "test": "echo 'No tests setup yet' && exit 0",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^4.0.0"
  }
}
